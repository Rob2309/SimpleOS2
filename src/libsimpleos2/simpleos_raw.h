#pragma once

#include "simpleos_types.h"

/**
 * @deprecated
 * Prints a string on the main virtual terminal.
 * Should not be used, as this function is only for OS testing purposes.
 **/
void print(const char* msg);